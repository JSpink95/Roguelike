#include "Game\Scripts\RotatingObject.h"

#include <Game\entity.h>
#include <Game\components.h>

void RotatingObject::OnUpdate(float deltaTime)
{
    //owner->GetComponent<TransformComponent>().rotation += deltaTime * rotationSpeed;
}